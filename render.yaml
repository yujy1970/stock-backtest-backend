services:
  - type: web
    name: stock-backtest-backend
    env: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    dockerContext: .
    autoDeploy: true
    # healthCheckPath: /actuator/health   # 有 Actuator 再开启
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -XX:MaxRAMPercentage=70.0
